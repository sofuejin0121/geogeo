<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= paginate @users, theme: 'bootstrap-5' %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= paginate @users, theme: 'bootstrap-5' %>
